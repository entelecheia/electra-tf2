# Specify the dataset to perform --action on
max_seq_length: 512
do_lower_case: 1 # Specify whether it is cased (0) or uncased (1) (any number greater than 0 will be treated as uncased)
vocab_file: ${data_dir}/vocab.txt
